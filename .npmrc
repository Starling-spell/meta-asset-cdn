# Hoist nothing implicitly — keep dependency graph honest across the monorepo.
# Next.js + many web3 SDKs ship ESM/CJS hybrids; auto-install peers smooths that over.
auto-install-peers=true
strict-peer-dependencies=false
# Required by some web3 toolchains that ship native/build scripts.
node-linker=isolated
